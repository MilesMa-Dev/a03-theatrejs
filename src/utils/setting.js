export const isDebug = true;

export const SIZE = {
    width: window.innerWidth,
    height: window.innerHeight
}

export const CameraConfig = {
    near: 0.1,
    far: 100
}

export const FPS = 60;
